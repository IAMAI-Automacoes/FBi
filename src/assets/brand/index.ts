// Logo oficial do Easy Feed.
// `easyFeedLogo` = lockup completo (ícone + texto "EASY FEED"), para cabeçalhos.
// `easyFeedIcon` = só o ícone (marca), para espaços quadrados (ex.: centro do QR).
import easyFeedLogo from './easy-feed-logo.png'
import easyFeedIcon from './easy-feed-icon.png'

export { easyFeedLogo, easyFeedIcon }
